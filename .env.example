# Optional — only for local `npm run creative-direction:weekly` (not used in CI).
# Weekly generation uses Cursor Automation; see creative-direction/agent-runbook.md
OPENAI_API_KEY=

# Optional: override models per pipeline step
# CREATIVE_DIRECTION_MODEL=gpt-4.1-mini
# CREATIVE_DIRECTION_DIRECTOR_MODEL=gpt-4.1
