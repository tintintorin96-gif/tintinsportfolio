import { HeroContent } from "@/components/hero/hero-shared";

export function CinematicHero() {
  return (
    <section className="hero-canvas relative mx-auto max-w-6xl px-6 py-24 md:py-32">
      <HeroContent />
    </section>
  );
}
